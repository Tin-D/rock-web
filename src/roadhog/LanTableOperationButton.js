class LanTableOperationButton {
    constructor ({ text, icon, type, handler, disabled }) {
        this.text = text;
        this.icon = icon;
        this.type = type;
        this.handler = handler;
        this.disabled = disabled;
    }
}

export default LanTableOperationButton;
